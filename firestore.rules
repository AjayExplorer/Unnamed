rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow unauthenticated reads and writes for development.
    // This is useful while the app is being built and debugged.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
